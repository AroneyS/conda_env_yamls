name: dirseq
channels:
  - conda-forge
  - bioconda
dependencies:
  - python>=3.6
  - ruby
  - samtools
  - bedtools>2.24
# gem install dirseq
# ln -sr $CONDA_PREFIX/bin/ruby $CONDA_PREFIX/share/rubygems/bin
