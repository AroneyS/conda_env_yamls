name: dirseq
channels:
  - conda-forge
  - bioconda
dependencies:
  - ruby=3.1.*
  - samtools
  - bedtools=2.24.*
# gem install dirseq
# ln -sr $CONDA_PREFIX/bin/ruby $CONDA_PREFIX/share/rubygems/bin
